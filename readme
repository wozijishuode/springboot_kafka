这个要和kafka_test一起看，主要时为了测试不同的序列化方式，能手顺利的接收数据。
在springboot——kafka中，它主要是消费者。我使用的是自定义的反序列化，
在kafka——test中，我使用的是字符串序列化，发送的是json字符串，结果不能接收，也不是不能接收，在接收的时候，我将它从object 转为jsonobject时，报错。所以，和人进行交互时，要提前说明自己接收的时字符串还是object。
